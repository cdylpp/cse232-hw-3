// Find the total amount borrowed by "John Smith". If he took out no loans, then output 0. 
// The output should be a table with a "borrowedAmount" column.

// DONE!
\rename_{borrowedAmount} (\aggr_{coalesce(sum(amount),0)} (loan \join_{no = lno} (\select_{cname = 'John Smith'} borrower)));
