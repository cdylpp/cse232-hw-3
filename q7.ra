// Find the loans with the maximal amount (there may be multiple tied loans, output all of them).

// DONE!

\rename_{loanNo}
\project_{no}
    (loan \join_{amount = maxAmount} \rename_{maxAmount} \aggr_{max(amount)} loan);